---
sudo: false
language: ruby
before_install:
  - gem update --system
  - rvm @default,@global do gem uninstall bundler -v 2.0.1 -x
cache: bundler
rvm:
  - 2.5
  - 2.6
  - ruby-head
matrix:
  fast_finish: true
  allow_failures:
    - rvm: ruby-head
install:
  - bundle install --jobs=3 --retry=3
script:
 — bundle exec rspec
